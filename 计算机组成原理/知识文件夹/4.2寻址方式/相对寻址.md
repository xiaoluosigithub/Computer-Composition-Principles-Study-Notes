相对寻址是指程序计数器（PC）的内容与指令中的形式地址A相加，形成转移目标地址，即$EA = (PC) + A$ 。其中，**PC为取指完成后自动更新的值，指向下一条指令的地址**；**A是相对于该PC值的偏移量，可正可负，通常以补码表示。**
![[Pasted image 20260515184837.png]]
**相对寻址主要用于转移类指令，形式地址A的位数决定了转移范围。**
![[Pasted image 20260515185052.png]]
优点是目标地址不固定，而是相对于当前指令位置偏移，因此程序可在内存中任意浮动而不影响转移正确性，便于实现重定位和共享代码。