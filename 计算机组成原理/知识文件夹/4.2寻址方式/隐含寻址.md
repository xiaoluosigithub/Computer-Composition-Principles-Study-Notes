**隐含寻址是指指令中不显式给出操作数地址，而将操作数地址隐含在特定寄存器中。**
例如：在累加器型结构中，单地址指令仅显式制定一个操作数地址，另一个操作数默认来自累加器（ACC）,运算结果也通常存回ACC
![[Pasted image 20260515180335.png]]
优点是可有效缩短指令字长
缺点是依赖存储隐含操作数的硬件（如ACC）
![[Pasted image 20260515184207.png]]