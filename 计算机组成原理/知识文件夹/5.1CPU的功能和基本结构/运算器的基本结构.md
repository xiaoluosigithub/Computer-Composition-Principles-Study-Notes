![[749aba2f29a3ab0e078c18745ff0ec69_720.jpg]]

1. 算术逻辑单元（ALU）：执行数据运算的核心部件，完成算术与逻辑运算。运算结果送回寄存器，状态标志则写入标志寄存器，供后续条件转移指令使用。
2. 通用寄存器组（GPRs）：供用户程序灵活使用，用于暂存操作数、中间结果或地址指针，减少对主存的频繁访问。==其位数等于指令字长。==
3. 标志寄存器（FR）：也称程序状态字寄存器（PSWR）。保存ALU运算产生的状态信息，用于条件判断与转移控制。这些标志位通常由触发器实现，整体构成程序状态字。
