1. CPU执行程序时，每当需要从主存取指令或者读/写数据，首先访问Cache
2. 若所需信息已在Cache中（称为Cache命中），则直接从Cache读取，无须访问主存
3. 若未命中（也称缺失），则需从主存中将该地址所在的一个主存块整体调入Cache，并将该块写入一个Cache行（若Cache已满，则按替换算法选择被替换块）
4. 此后CPU再从Cache中获取所需数据。==整个访问过程（包括命中判断、块调入、替换等）必须在单条指令周期内完成，因此完全由硬件实现。==
**Cache机制对程序员是透明的**
