1. 首先根据访存地址中的组号字段确定目标Cache组；将该组内所有Cache行的标记与主存地址的高位标记进行比较。
	1. 若某行标记匹配且有效位为1，则Cache命中，根据块内地址从该行读取数据
	2. 若所有行均不匹配或匹配行的有效位为0，则Cache未命中，CPU从主存读取该地址所在块，将其装入该组中的任意一个空闲行（若无空闲行，则按替换算法选择一行，置有效位为1，写入标记，并将所需数据送至CPU）