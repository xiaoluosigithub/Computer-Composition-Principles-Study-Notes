主存中的每一块只能装入Cache中唯一指定的位置。
若该位置已有内容，则发生块冲突，原块将被无条件替换

**直接映射最简单，但灵活性差**，即使Cache中其他行空闲，也不能用于存放该主存块，因此 **块冲突概率最高，空间利用率最低**

设Cache共有 $2^c$ 行，主存共有 $2^m$ 块
**主存块号的最低c位即为对应的Cache行号**。主存块号的其他位为对应的标记。

![[Pasted image 20260511204444.png]]