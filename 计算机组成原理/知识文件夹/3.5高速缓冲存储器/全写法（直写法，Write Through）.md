当CPU对Cache写命中时，数据同时写入Cache和主存。
由于主存始终与Cache保持同步，因此在替换Cache块时，可直接覆盖，无需写回。
该方法实现简单，能始终保持主存数据的实时正确性，但缺点是每次写操作都要访问主存，降低了系统性能

为缓解全写法的性能开销，可在Cache与主存之间增设写缓冲（Write Buffer）
CPU将数据同时写入Cache和写缓冲，由写缓冲异步地将数据写入主存。![[Pasted image 20260512094937.png#pic_center|441]]
写缓冲可缓解CPU与主存之间的速度差异。但在高频率写操作下，写缓冲可能饱和甚至溢出