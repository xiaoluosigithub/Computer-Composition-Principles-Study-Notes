在计算Cache总容量时，需考虑到Cache行的数据部分和每行的标记信息
$$Cache总容量 = (每行标记位数 + 每行数据位数) * Cache总行数$$
每行的标记信息通常包括：
$$有效位、标记位、脏位、LRU替换位$$
有效位和标记位是所有Cache必须包含的
脏位仅在采用回写法时存在
LRU替换位仅在使用LRU算法时存在，其位数取决于组内行数。
![[Pasted image 20260512221002.png]]
