异常是由CPU内部产生的意外事件，可分为**硬故障中断**和**程序性异常**。
硬故障中断是由**硬件逻辑功能出现异常**引起的
- 如存储器校验错、总线错误等。

**程序性异常**也称**软件中断**，是指在CPU内部因**执行指令而引起的异常**事件。
- 如：除零、溢出、断点、单步跟踪、非法指令、栈溢出、地址越界、缺页等。

按异常发生原因和返回方式的不同，可分为**故障、自陷和终止**。