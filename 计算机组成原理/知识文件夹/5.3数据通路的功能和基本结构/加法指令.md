![[Pasted image 20260520191621.png]]
![[Pasted image 20260520191637.png]]
**在单总线数据通路中，任一时刻总线上仅允许一个数据传输。由于ALU是无存储能力的组合逻辑电路，运算时需要两个输入同时有效，因此需要先将一个操作数经总线送入暂存寄存器Y**；下一个周期再将另一个操作数送至ALU的另一个输入端，Y的输出即作为第一个操作数。
**ALU输出不能直接连到总线，否则可能通过总线反馈至输入端，干扰运算结果，故需要暂存于暂存寄存器Z中**