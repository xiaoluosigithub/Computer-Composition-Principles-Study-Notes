不论**CPU的内部结构**多么复杂，都可视为由数据通路（Data Path）和控制部件（Control Unit）两大部分组成。
==数据在指令执行过程中所经过的路径，以及路径上设计的硬件部件，称为数据通路==
ALU、通用寄存器、状态寄存器等，都是**指令执行时数据流经的部件**，属于数据通路的一部分。
数据通路描述了数据从何开始，中间经过哪些部件，最终被传送到哪里。
**整个数据通路由控制部件控制**，后者**根据每条指令的功能，生成相应的控制信号**，以驱动数据通路完成指定操作。
